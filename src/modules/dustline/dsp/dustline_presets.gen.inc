/* GENERATED by scripts/gen-presets.ts from presets.json — do not edit. */
/* Regenerate with: mise run gen-presets (or: pnpm run gen-presets) */

#ifndef DUSTLINE_PRESETS_GEN_INC
#define DUSTLINE_PRESETS_GEN_INC

#define DUSTLINE_PRESET_COUNT 6

typedef struct {
    const char *name;
    const float *values;
} dustline_preset_t;

static const char *dustline_preset_param_keys[9] = {
    "volume",
    "wave",
    "noise",
    "cutoff",
    "resonance",
    "attack",
    "release",
    "drive",
    "bend_range"
};

static const float dustline_preset_values_0[9] = { 0.7f, 0.28f, 0.08f, 0.48f, 0.16f, 0.006f, 0.42f, 0.12f, 2.0f };
static const float dustline_preset_values_1[9] = { 0.88f, 0.92f, 0.03f, 0.24f, 0.08f, 0.003f, 0.18f, 0.74f, 2.0f };
static const float dustline_preset_values_2[9] = { 0.56f, 0.12f, 0.82f, 0.7f, 0.56f, 0.22f, 3.2f, 0.12f, 2.0f };
static const float dustline_preset_values_3[9] = { 0.72f, 0.58f, 0.01f, 0.86f, 0.3f, 0.002f, 0.14f, 0.32f, 7.0f };
static const float dustline_preset_values_4[9] = { 0.66f, 0.42f, 0.04f, 0.62f, 0.64f, 0.012f, 0.72f, 0.08f, 2.0f };
static const float dustline_preset_values_5[9] = { 0.62f, 0.72f, 0.24f, 0.36f, 0.86f, 0.32f, 2.4f, 0.42f, 2.0f };

static const dustline_preset_t dustline_presets[6] = {
    { "Init", dustline_preset_values_0 },
    { "Dust Bass", dustline_preset_values_1 },
    { "Air Noise", dustline_preset_values_2 },
    { "Pin Lead", dustline_preset_values_3 },
    { "Glass Keys", dustline_preset_values_4 },
    { "Filter Sweep", dustline_preset_values_5 }
};

static int dustline_preset_count(void) {
    return DUSTLINE_PRESET_COUNT;
}

static const char *dustline_preset_name(int index) {
    if (index < 0 || index >= DUSTLINE_PRESET_COUNT) return "";
    return dustline_presets[index].name;
}

static int dustline_clamp_preset_index(int index) {
    if (DUSTLINE_PRESET_COUNT <= 0) return 0;
    if (index < 0) return 0;
    if (index >= DUSTLINE_PRESET_COUNT) return DUSTLINE_PRESET_COUNT - 1;
    return index;
}

static void dustline_apply_preset(dustline_core_t *s, int index) {
    if (!s || DUSTLINE_PRESET_COUNT <= 0) return;
    index = dustline_clamp_preset_index(index);
    const float *values = dustline_presets[index].values;
    if (!values) return;
    for (int i = 0; i < 9; i++) {
        dustline_set_param(s, dustline_param_id(dustline_preset_param_keys[i]), values[i]);
    }
}

#endif
