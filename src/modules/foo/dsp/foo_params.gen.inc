/* GENERATED by scripts/gen-params.ts from module.json — do not edit. */
/* Regenerate with: mise run gen-params (or: pnpm run gen-params) */

#ifndef FOO_PARAMS_GEN_INC
#define FOO_PARAMS_GEN_INC

enum {
    FOO_PARAM_TIME = 0,
    FOO_PARAM_FEEDBACK = 1,
    FOO_PARAM_MIX = 2
};

static inline float foo_params_clampf_(float x, float lo, float hi) {
    return x < lo ? lo : (x > hi ? hi : x);
}

int foo_param_id(const char *key) {
    if (!key) return -1;
    if (strcmp(key, "time") == 0) return FOO_PARAM_TIME;
    if (strcmp(key, "feedback") == 0) return FOO_PARAM_FEEDBACK;
    if (strcmp(key, "mix") == 0) return FOO_PARAM_MIX;
    return -1;
}

void foo_set_param(foo_core_t *s, int param_id, float value) {
    if (!s) return;
    switch (param_id) {
        case FOO_PARAM_TIME: s->time = foo_params_clampf_(value, 0.0f, 1.0f); break;
        case FOO_PARAM_FEEDBACK: s->feedback = foo_params_clampf_(value, 0.0f, 0.95f); break;
        case FOO_PARAM_MIX: s->mix = foo_params_clampf_(value, 0.0f, 1.0f); break;
        default: break;
    }
}

float foo_get_param(const foo_core_t *s, int param_id) {
    if (!s) return 0.0f;
    switch (param_id) {
        case FOO_PARAM_TIME: return s->time;
        case FOO_PARAM_FEEDBACK: return s->feedback;
        case FOO_PARAM_MIX: return s->mix;
        default: return 0.0f;
    }
}

void foo_apply_defaults(foo_core_t *s) {
    if (!s) return;
    s->time = 0.25f;
    s->feedback = 0.35f;
    s->mix = 0.35f;
}

#endif
