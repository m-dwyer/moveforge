/* GENERATED by scripts/gen-params.ts from module.json — do not edit. */
/* Regenerate with: mise run gen-params (or: pnpm run gen-params) */

#ifndef VELO_SCALE_PARAMS_GEN_INC
#define VELO_SCALE_PARAMS_GEN_INC

enum {
    VELO_SCALE_PARAM_SCALE = 0
};

static inline float velo_scale_params_clampf_(float x, float lo, float hi) {
    return x < lo ? lo : (x > hi ? hi : x);
}

int velo_scale_param_id(const char *key) {
    if (!key) return -1;
    if (strcmp(key, "scale") == 0) return VELO_SCALE_PARAM_SCALE;
    return -1;
}

void velo_scale_set_param(velo_scale_core_t *s, int param_id, float value) {
    if (!s) return;
    switch (param_id) {
        case VELO_SCALE_PARAM_SCALE: s->scale = velo_scale_params_clampf_(value, 0.0f, 2.0f); break;
        default: break;
    }
}

float velo_scale_get_param(const velo_scale_core_t *s, int param_id) {
    if (!s) return 0.0f;
    switch (param_id) {
        case VELO_SCALE_PARAM_SCALE: return s->scale;
        default: return 0.0f;
    }
}

void velo_scale_apply_defaults(velo_scale_core_t *s) {
    if (!s) return;
    s->scale = 1.0f;
}

#endif
