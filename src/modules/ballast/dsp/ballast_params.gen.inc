/* GENERATED by scripts/gen-params.ts from module.json — do not edit. */
/* Regenerate with: mise run gen-params (or: pnpm run gen-params) */

#ifndef BALLAST_PARAMS_GEN_INC
#define BALLAST_PARAMS_GEN_INC

/* BALLAST_PARAM_COUNT and the BALLAST_PARAM_* enum. Also included by
 * ballast_core.h; the guard makes the second include a no-op. */
#include "ballast_params.gen.h"

static inline float ballast_params_clampf_(float x, float lo, float hi) {
    return x < lo ? lo : (x > hi ? hi : x);
}

int ballast_param_id(const char *key) {
    if (!key) return -1;
    if (strcmp(key, "tune") == 0) return BALLAST_PARAM_TUNE;
    if (strcmp(key, "punch") == 0) return BALLAST_PARAM_PUNCH;
    if (strcmp(key, "drop") == 0) return BALLAST_PARAM_DROP;
    if (strcmp(key, "decay") == 0) return BALLAST_PARAM_DECAY;
    if (strcmp(key, "drive") == 0) return BALLAST_PARAM_DRIVE;
    if (strcmp(key, "tone") == 0) return BALLAST_PARAM_TONE;
    if (strcmp(key, "dirt") == 0) return BALLAST_PARAM_DIRT;
    if (strcmp(key, "volume") == 0) return BALLAST_PARAM_VOLUME;
    if (strcmp(key, "track") == 0) return BALLAST_PARAM_TRACK;
    if (strcmp(key, "sweep") == 0) return BALLAST_PARAM_SWEEP;
    if (strcmp(key, "shape") == 0) return BALLAST_PARAM_SHAPE;
    if (strcmp(key, "phase") == 0) return BALLAST_PARAM_PHASE;
    if (strcmp(key, "curve") == 0) return BALLAST_PARAM_CURVE;
    if (strcmp(key, "body") == 0) return BALLAST_PARAM_BODY;
    if (strcmp(key, "vel_depth") == 0) return BALLAST_PARAM_VEL_DEPTH;
    if (strcmp(key, "human") == 0) return BALLAST_PARAM_HUMAN;
    return -1;
}

void ballast_set_param(ballast_core_t *s, int param_id, float value) {
    if (!s) return;
    switch (param_id) {
        case BALLAST_PARAM_TUNE: s->tune = ballast_params_clampf_(value, 24.0f, 72.0f); break;
        case BALLAST_PARAM_PUNCH: s->punch = ballast_params_clampf_(value, 0.0f, 1.0f); break;
        case BALLAST_PARAM_DROP: s->drop = ballast_params_clampf_(value, 0.0f, 1.0f); break;
        case BALLAST_PARAM_DECAY: s->decay = ballast_params_clampf_(value, 0.05f, 3.0f); break;
        case BALLAST_PARAM_DRIVE: s->drive = ballast_params_clampf_(value, 0.0f, 1.0f); break;
        case BALLAST_PARAM_TONE: s->tone = ballast_params_clampf_(value, 0.0f, 1.0f); break;
        case BALLAST_PARAM_DIRT: s->dirt = ballast_params_clampf_(value, 0.0f, 1.0f); break;
        case BALLAST_PARAM_VOLUME: s->volume = ballast_params_clampf_(value, 0.0f, 1.0f); break;
        case BALLAST_PARAM_TRACK: s->track = ballast_params_clampf_(value, 0.0f, 1.0f); break;
        case BALLAST_PARAM_SWEEP: s->sweep = ballast_params_clampf_(value, 0.0f, 1.0f); break;
        case BALLAST_PARAM_SHAPE: s->shape = ballast_params_clampf_(value, 0.0f, 1.0f); break;
        case BALLAST_PARAM_PHASE: s->phase = ballast_params_clampf_(value, 0.0f, 1.0f); break;
        case BALLAST_PARAM_CURVE: s->curve = ballast_params_clampf_(value, 0.0f, 4.0f); break;
        case BALLAST_PARAM_BODY: s->body = ballast_params_clampf_(value, 0.0f, 1.0f); break;
        case BALLAST_PARAM_VEL_DEPTH: s->vel_depth = ballast_params_clampf_(value, 0.0f, 1.0f); break;
        case BALLAST_PARAM_HUMAN: s->human = ballast_params_clampf_(value, 0.0f, 1.0f); break;
        default: break;
    }
}

float ballast_get_param(const ballast_core_t *s, int param_id) {
    if (!s) return 0.0f;
    switch (param_id) {
        case BALLAST_PARAM_TUNE: return s->tune;
        case BALLAST_PARAM_PUNCH: return s->punch;
        case BALLAST_PARAM_DROP: return s->drop;
        case BALLAST_PARAM_DECAY: return s->decay;
        case BALLAST_PARAM_DRIVE: return s->drive;
        case BALLAST_PARAM_TONE: return s->tone;
        case BALLAST_PARAM_DIRT: return s->dirt;
        case BALLAST_PARAM_VOLUME: return s->volume;
        case BALLAST_PARAM_TRACK: return s->track;
        case BALLAST_PARAM_SWEEP: return s->sweep;
        case BALLAST_PARAM_SHAPE: return s->shape;
        case BALLAST_PARAM_PHASE: return s->phase;
        case BALLAST_PARAM_CURVE: return s->curve;
        case BALLAST_PARAM_BODY: return s->body;
        case BALLAST_PARAM_VEL_DEPTH: return s->vel_depth;
        case BALLAST_PARAM_HUMAN: return s->human;
        default: return 0.0f;
    }
}

void ballast_apply_defaults(ballast_core_t *s) {
    if (!s) return;
    s->tune = 33.0f;
    s->punch = 0.5f;
    s->drop = 0.45f;
    s->decay = 0.45f;
    s->drive = 0.3f;
    s->tone = 0.5f;
    s->dirt = 0.0f;
    s->volume = 0.75f;
    s->track = 0.0f;
    s->sweep = 0.45f;
    s->shape = 0.35f;
    s->phase = 0.0f;
    s->curve = 0.0f;
    s->body = 0.2f;
    s->vel_depth = 0.6f;
    s->human = 0.0f;
}

#endif
