/* GENERATED by scripts/gen-presets.ts from presets.json — do not edit. */
/* Regenerate with: mise run gen-presets (or: pnpm run gen-presets) */

#ifndef ARPY_PRESETS_GEN_INC
#define ARPY_PRESETS_GEN_INC

#define ARPY_PRESET_COUNT 4

typedef struct {
    const char *name;
    const float *values;
} arpy_preset_t;

static const char *arpy_preset_param_keys[3] = {
    "pattern",
    "chord",
    "rate"
};

static const float arpy_preset_values_0[3] = { 0.0f, 3.0f, 0.3f };
static const float arpy_preset_values_1[3] = { 1.0f, 3.0f, 0.3f };
static const float arpy_preset_values_2[3] = { 3.0f, 2.0f, 0.4f };
static const float arpy_preset_values_3[3] = { 1.0f, 4.0f, 0.2f };

static const arpy_preset_t arpy_presets[4] = {
    { "Off", arpy_preset_values_0 },
    { "Triad Up", arpy_preset_values_1 },
    { "Power Up-Down", arpy_preset_values_2 },
    { "Dom7 Up", arpy_preset_values_3 }
};

static int arpy_preset_count(void) {
    return ARPY_PRESET_COUNT;
}

static const char *arpy_preset_name(int index) {
    if (index < 0 || index >= ARPY_PRESET_COUNT) return "";
    return arpy_presets[index].name;
}

static int arpy_clamp_preset_index(int index) {
    if (ARPY_PRESET_COUNT <= 0) return 0;
    if (index < 0) return 0;
    if (index >= ARPY_PRESET_COUNT) return ARPY_PRESET_COUNT - 1;
    return index;
}

static void arpy_apply_preset(arpy_core_t *s, int index) {
    if (!s || ARPY_PRESET_COUNT <= 0) return;
    index = arpy_clamp_preset_index(index);
    const float *values = arpy_presets[index].values;
    if (!values) return;
    for (int i = 0; i < 3; i++) {
        arpy_set_param(s, arpy_param_id(arpy_preset_param_keys[i]), values[i]);
    }
}

#endif
