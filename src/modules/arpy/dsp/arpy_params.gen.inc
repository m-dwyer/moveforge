/* GENERATED by scripts/gen-params.ts from module.json — do not edit. */
/* Regenerate with: mise run gen-params (or: pnpm run gen-params) */

#ifndef ARPY_PARAMS_GEN_INC
#define ARPY_PARAMS_GEN_INC

enum {
    ARPY_PARAM_PATTERN = 0,
    ARPY_PARAM_CHORD = 1,
    ARPY_PARAM_RATE = 2
};

static inline float arpy_params_clampf_(float x, float lo, float hi) {
    return x < lo ? lo : (x > hi ? hi : x);
}

int arpy_param_id(const char *key) {
    if (!key) return -1;
    if (strcmp(key, "pattern") == 0) return ARPY_PARAM_PATTERN;
    if (strcmp(key, "chord") == 0) return ARPY_PARAM_CHORD;
    if (strcmp(key, "rate") == 0) return ARPY_PARAM_RATE;
    return -1;
}

void arpy_set_param(arpy_core_t *s, int param_id, float value) {
    if (!s) return;
    switch (param_id) {
        case ARPY_PARAM_PATTERN: s->pattern = arpy_params_clampf_(value, 0.0f, 3.0f); break;
        case ARPY_PARAM_CHORD: s->chord = arpy_params_clampf_(value, 0.0f, 4.0f); break;
        case ARPY_PARAM_RATE: s->rate = arpy_params_clampf_(value, 0.0f, 1.0f); break;
        default: break;
    }
}

float arpy_get_param(const arpy_core_t *s, int param_id) {
    if (!s) return 0.0f;
    switch (param_id) {
        case ARPY_PARAM_PATTERN: return s->pattern;
        case ARPY_PARAM_CHORD: return s->chord;
        case ARPY_PARAM_RATE: return s->rate;
        default: return 0.0f;
    }
}

void arpy_apply_defaults(arpy_core_t *s) {
    if (!s) return;
    s->pattern = 1.0f;
    s->chord = 3.0f;
    s->rate = 0.3f;
}

#endif
