/* GENERATED by scripts/gen-params.ts from module.json — do not edit. */
/* Regenerate with: mise run gen-params (or: pnpm run gen-params) */

#ifndef VCA_PARAMS_GEN_INC
#define VCA_PARAMS_GEN_INC

/* VCA_PARAM_COUNT and the VCA_PARAM_* enum. Also included by
 * vca_core.h; the guard makes the second include a no-op. */
#include "vca_params.gen.h"

static inline float vca_params_clampf_(float x, float lo, float hi) {
    return x < lo ? lo : (x > hi ? hi : x);
}

int vca_param_id(const char *key) {
    if (!key) return -1;
    if (strcmp(key, "attack") == 0) return VCA_PARAM_ATTACK;
    if (strcmp(key, "decay") == 0) return VCA_PARAM_DECAY;
    if (strcmp(key, "sustain") == 0) return VCA_PARAM_SUSTAIN;
    if (strcmp(key, "release") == 0) return VCA_PARAM_RELEASE;
    return -1;
}

void vca_set_param(vca_core_t *s, int param_id, float value) {
    if (!s) return;
    switch (param_id) {
        case VCA_PARAM_ATTACK: s->attack = vca_params_clampf_(value, 0.0f, 2.0f); break;
        case VCA_PARAM_DECAY: s->decay = vca_params_clampf_(value, 0.0f, 4.0f); break;
        case VCA_PARAM_SUSTAIN: s->sustain = vca_params_clampf_(value, 0.0f, 1.0f); break;
        case VCA_PARAM_RELEASE: s->release = vca_params_clampf_(value, 0.0f, 6.0f); break;
        default: break;
    }
}

float vca_get_param(const vca_core_t *s, int param_id) {
    if (!s) return 0.0f;
    switch (param_id) {
        case VCA_PARAM_ATTACK: return s->attack;
        case VCA_PARAM_DECAY: return s->decay;
        case VCA_PARAM_SUSTAIN: return s->sustain;
        case VCA_PARAM_RELEASE: return s->release;
        default: return 0.0f;
    }
}

void vca_apply_defaults(vca_core_t *s) {
    if (!s) return;
    s->attack = 0.0f;
    s->decay = 0.0f;
    s->sustain = 1.0f;
    s->release = 6.0f;
}

#endif
