/* GENERATED by scripts/gen-presets.ts from presets.json — do not edit. */
/* Regenerate with: mise run gen-presets (or: pnpm run gen-presets) */

#ifndef TRAIL_PRESETS_GEN_INC
#define TRAIL_PRESETS_GEN_INC

#define TRAIL_PRESET_COUNT 6

typedef struct {
    const char *name;
    const float *values;
} trail_preset_t;

static const char *trail_preset_param_keys[9] = {
    "time",
    "sync",
    "feedback",
    "tone",
    "mod",
    "width",
    "drive",
    "space",
    "mix"
};

static const float trail_preset_values_0[9] = { 0.12f, 0.0f, 0.22f, 0.55f, 0.12f, 0.45f, 0.12f, 0.05f, 0.18f };
static const float trail_preset_values_1[9] = { 0.05f, 0.0f, 0.12f, 0.85f, 0.03f, 0.18f, 0.08f, 0.03f, 0.28f };
static const float trail_preset_values_2[9] = { 0.32f, 0.0f, 0.52f, 0.22f, 0.18f, 0.55f, 0.32f, 0.12f, 0.25f };
static const float trail_preset_values_3[9] = { 0.3f, 5.0f, 0.62f, 0.2f, 0.15f, 0.9f, 0.2f, 0.18f, 0.28f };
static const float trail_preset_values_4[9] = { 0.18f, 0.0f, 0.42f, 0.48f, 0.75f, 0.24f, 0.45f, 0.08f, 0.24f };
static const float trail_preset_values_5[9] = { 0.3f, 6.0f, 0.34f, 0.7f, 0.2f, 0.7f, 0.16f, 0.85f, 0.25f };

static const trail_preset_t trail_presets[6] = {
    { "Init", trail_preset_values_0 },
    { "Slap", trail_preset_values_1 },
    { "Dub", trail_preset_values_2 },
    { "Hypno Dub", trail_preset_values_3 },
    { "Tape Wobble", trail_preset_values_4 },
    { "Cavern", trail_preset_values_5 }
};

static int trail_preset_count(void) {
    return TRAIL_PRESET_COUNT;
}

static const char *trail_preset_name(int index) {
    if (index < 0 || index >= TRAIL_PRESET_COUNT) return "";
    return trail_presets[index].name;
}

static int trail_clamp_preset_index(int index) {
    if (TRAIL_PRESET_COUNT <= 0) return 0;
    if (index < 0) return 0;
    if (index >= TRAIL_PRESET_COUNT) return TRAIL_PRESET_COUNT - 1;
    return index;
}

static void trail_apply_preset(trail_core_t *s, int index) {
    if (!s || TRAIL_PRESET_COUNT <= 0) return;
    index = trail_clamp_preset_index(index);
    const float *values = trail_presets[index].values;
    if (!values) return;
    for (int i = 0; i < 9; i++) {
        trail_set_param(s, trail_param_id(trail_preset_param_keys[i]), values[i]);
    }
}

#endif
