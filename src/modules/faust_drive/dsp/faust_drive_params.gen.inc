/* GENERATED by scripts/gen-params.ts from module.json — do not edit. */
/* Regenerate with: mise run gen-params (or: pnpm run gen-params) */

#ifndef FAUST_DRIVE_PARAMS_GEN_INC
#define FAUST_DRIVE_PARAMS_GEN_INC

enum {
    FAUST_DRIVE_PARAM_DRIVE = 0,
    FAUST_DRIVE_PARAM_TONE = 1,
    FAUST_DRIVE_PARAM_MIX = 2,
    FAUST_DRIVE_PARAM_LEVEL = 3
};

static inline float faust_drive_params_clampf_(float x, float lo, float hi) {
    return x < lo ? lo : (x > hi ? hi : x);
}

int faust_drive_param_id(const char *key) {
    if (!key) return -1;
    if (strcmp(key, "drive") == 0) return FAUST_DRIVE_PARAM_DRIVE;
    if (strcmp(key, "tone") == 0) return FAUST_DRIVE_PARAM_TONE;
    if (strcmp(key, "mix") == 0) return FAUST_DRIVE_PARAM_MIX;
    if (strcmp(key, "level") == 0) return FAUST_DRIVE_PARAM_LEVEL;
    return -1;
}

void faust_drive_set_param(faust_drive_core_t *s, int param_id, float value) {
    if (!s) return;
    switch (param_id) {
        case FAUST_DRIVE_PARAM_DRIVE: s->drive = faust_drive_params_clampf_(value, 0.0f, 1.0f); break;
        case FAUST_DRIVE_PARAM_TONE: s->tone = faust_drive_params_clampf_(value, 0.0f, 1.0f); break;
        case FAUST_DRIVE_PARAM_MIX: s->mix = faust_drive_params_clampf_(value, 0.0f, 1.0f); break;
        case FAUST_DRIVE_PARAM_LEVEL: s->level = faust_drive_params_clampf_(value, 0.0f, 1.0f); break;
        default: break;
    }
}

float faust_drive_get_param(const faust_drive_core_t *s, int param_id) {
    if (!s) return 0.0f;
    switch (param_id) {
        case FAUST_DRIVE_PARAM_DRIVE: return s->drive;
        case FAUST_DRIVE_PARAM_TONE: return s->tone;
        case FAUST_DRIVE_PARAM_MIX: return s->mix;
        case FAUST_DRIVE_PARAM_LEVEL: return s->level;
        default: return 0.0f;
    }
}

void faust_drive_apply_defaults(faust_drive_core_t *s) {
    if (!s) return;
    s->drive = 0.3f;
    s->tone = 0.5f;
    s->mix = 1.0f;
    s->level = 0.8f;
}

#endif
