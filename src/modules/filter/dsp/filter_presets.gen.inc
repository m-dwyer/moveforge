/* GENERATED by scripts/gen-presets.ts from presets.json — do not edit. */
/* Regenerate with: mise run gen-presets (or: pnpm run gen-presets) */

#ifndef FILTER_PRESETS_GEN_INC
#define FILTER_PRESETS_GEN_INC

#define FILTER_PRESET_COUNT 5

typedef struct {
    const char *name;
    const float *values;
} filter_preset_t;

static const char *filter_preset_param_keys[3] = {
    "cutoff",
    "resonance",
    "morph"
};

static const float filter_preset_values_0[3] = { 1.0f, 0.0f, 0.0f };
static const float filter_preset_values_1[3] = { 0.45f, 0.3f, 0.0f };
static const float filter_preset_values_2[3] = { 0.3f, 0.85f, 0.0f };
static const float filter_preset_values_3[3] = { 0.55f, 0.5f, 0.5f };
static const float filter_preset_values_4[3] = { 0.35f, 0.25f, 1.0f };

static const filter_preset_t filter_presets[5] = {
    { "Open", filter_preset_values_0 },
    { "Lowpass", filter_preset_values_1 },
    { "Squelch", filter_preset_values_2 },
    { "Bandpass", filter_preset_values_3 },
    { "Highpass", filter_preset_values_4 }
};

static int filter_preset_count(void) {
    return FILTER_PRESET_COUNT;
}

static const char *filter_preset_name(int index) {
    if (index < 0 || index >= FILTER_PRESET_COUNT) return "";
    return filter_presets[index].name;
}

static int filter_clamp_preset_index(int index) {
    if (FILTER_PRESET_COUNT <= 0) return 0;
    if (index < 0) return 0;
    if (index >= FILTER_PRESET_COUNT) return FILTER_PRESET_COUNT - 1;
    return index;
}

static void filter_apply_preset(filter_core_t *s, int index) {
    if (!s || FILTER_PRESET_COUNT <= 0) return;
    index = filter_clamp_preset_index(index);
    const float *values = filter_presets[index].values;
    if (!values) return;
    for (int i = 0; i < 3; i++) {
        filter_set_param(s, filter_param_id(filter_preset_param_keys[i]), values[i]);
    }
}

#endif
