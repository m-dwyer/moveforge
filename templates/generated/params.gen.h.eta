/* GENERATED by scripts/gen-params.ts from module.json — do not edit. */
/* Regenerate with: mise run gen-params (or: pnpm run gen-params) */

/* Split out of <%= it.moduleId %>_params.gen.inc so the param count and enum are
 * visible to <%= it.moduleId %>_core.h, which needs <%= it.upper %>_PARAM_COUNT to size
 * arrays that are indexed by param id. The .inc cannot serve that purpose: it
 * defines functions taking <%= it.coreType %>*, so it has to be included *after* the
 * struct, which is too late. */

#ifndef <%= it.guard %>
#define <%= it.guard %>

#define <%= it.upper %>_PARAM_COUNT <%= it.params.length %>

enum {
<% it.params.forEach((p, i) => { -%>
    <%= it.enumName(p.key) %> = <%= i %><%= i < it.params.length - 1 ? "," : "" %>
<% }) -%>
};

#endif
