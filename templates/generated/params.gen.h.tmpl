/* GENERATED by scripts/gen-params.ts from module.json — do not edit. */
/* Regenerate with: mise run gen-params (or: pnpm run gen-params) */

/* Split out of {{moduleId}}_params.gen.inc so the param count and enum are
 * visible to {{moduleId}}_core.h, which needs {{upper}}_PARAM_COUNT to size
 * arrays that are indexed by param id. The .inc cannot serve that purpose: it
 * defines functions taking {{coreType}}*, so it has to be included *after* the
 * struct, which is too late. */

#ifndef {{guard}}
#define {{guard}}

#define {{upper}}_PARAM_COUNT {{paramCount}}

enum {
{{enumEntries}}
};

#endif
