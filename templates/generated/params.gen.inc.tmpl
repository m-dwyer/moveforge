/* GENERATED by scripts/gen-params.ts from module.json — do not edit. */
/* Regenerate with: mise run gen-params (or: pnpm run gen-params) */

#ifndef {{guard}}
#define {{guard}}

#define {{upper}}_PARAM_COUNT {{paramCount}}

enum {
{{enumEntries}}
};

static inline float {{moduleId}}_params_clampf_(float x, float lo, float hi) {
    return x < lo ? lo : (x > hi ? hi : x);
}

int {{moduleId}}_param_id(const char *key) {
    if (!key) return -1;
{{idLookup}}
    return -1;
}

void {{moduleId}}_set_param({{coreType}} *s, int param_id, float value) {
    if (!s) return;
    switch (param_id) {
{{setCases}}
        default: break;
    }
}

float {{moduleId}}_get_param(const {{coreType}} *s, int param_id) {
    if (!s) return 0.0f;
    switch (param_id) {
{{getCases}}
        default: return 0.0f;
    }
}

void {{moduleId}}_apply_defaults({{coreType}} *s) {
    if (!s) return;
{{defaults}}
}

#endif
